<p id="notice"><%= notice %></p>

<h1>Terms</h1>

<div class="table-responsive">
  <table class="table table-bordered table-condensed table-hover">
    <thead>
      <tr>
        <th>Name</th>
        <th>Definition</th>
        <th>Author</th>
        <th>Resource</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @terms.each do |term| %>
        <tr>
          <td><%= term.name %></td>
          <td><%= term.definition %></td>
          <td><%= term.author %></td>
          <td><%= term.resource %></td>
          <td><%= link_to 'Show', term %></td>
          <td>
            <% if term.authorized?(current_user) %>
              <%= link_to edit_term_path(term) do %>
                <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
              <% end %>
            <% end %>
          </td>
          <td>
            <% if term.authorized?(current_user) %>
              <%= link_to term, method: :delete, data: { confirm: 'You really want to do this?' } do %>
                <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
              <% end %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
<br>

<%= link_to 'New Term', new_term_path, class: "btn btn-default" %>
